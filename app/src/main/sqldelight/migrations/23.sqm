CREATE TABLE excluded_scanlators(
    _id INTEGER NOT NULL PRIMARY KEY,
    manga_id INTEGAR NOT NULL,
    scanlator TEXT NOT NULL,
    FOREIGN KEY(manga_id) REFERENCES mangas (_id)
    ON DELETE CASCADE
);
